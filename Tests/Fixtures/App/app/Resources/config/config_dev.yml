imports:
    - { resource: config.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.filesystem

khepin_yaml_fixtures:
    directory: Resources/Fixtures
    resources:
        - VirhiLazyRestApiBundle/Post
