build_settings:
    ignore:
        - "vendor"

setup:
    composer:
        action: "install"
    shell:
        - "./init.sh"

test:
    php_unit:
        config:
            - "./phpunit.xml"
        run_from: "./"
        coverage: "build/logs/coverage"

    behat:
        features:

