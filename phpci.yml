build_settings:
    ignore:
        - "vendor"

setup:
    composer:
        action: "install"
    shell: sh init.sh

test:
    php_unit:
        config:
            - "./phpunit.xml"
        run_from: "./"
        coverage: "build/logs/coverage"

    features:

